# -*- mode: snippet -*-
# name: ss-comp
# key: ss-comp
# --
import React from 'react';

import { block } from 'utils/classname';

import './style.scss';

const b = block('`(camel-case->lisp-case (containing-directory))`');

type Props = {}

function `(containing-directory)`({}: Props) {
  return (
    <div className={b()}>
      `(containing-directory)`
    </div>
  );
}

export const Component = React.memo(`(containing-directory)`) as typeof `(containing-directory)`;